scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node_backend'
    static_configs:
      - targets: ['localhost:5000']  # لازم يكون نفس البورت اللي السيرفر شغال عليه
